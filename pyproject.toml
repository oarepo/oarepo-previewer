[project]
name = "oarepo-previewer"
version = "0.1.0"
description = "OARepo contrib previewers for invenio-previewer (custom file extensions)."
readme = "README.md"
requires-python = ">=3.13,<3.15"
dependencies = [
    "invenio-previewer>=5.0.0,<6.0.0",
]

[project.urls]
Homepage = "https://github.com/oarepo/oarepo-previewer"

[project.optional-dependencies]
dev = [
    "ruff",
]
tests = [
    "pytest>=7.1.2",
    "pytest_oarepo>=6.0.0",
]
oarepo14 = [
    "oarepo[rdm,tests]>=14.0.0,<15.0.0",
]

[project.entry-points."invenio_base.apps"]
oarepo_previewer = "oarepo_previewer.ext:OARepoPreviewerExt"

[project.entry-points."invenio_previewer.previewers"]
mol = "oarepo_previewer.previewers.mol"

[project.entry-points."invenio_assets.webpack"]
oarepo_previewer_theme = "oarepo_previewer.theme.webpack:theme"

[project.entry-points."invenio_i18n.translations"]
oarepo_previewer_messages = "oarepo_previewer"

[tool.hatch.build.targets.sdist]
include = [
    "/oarepo_previewer",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
